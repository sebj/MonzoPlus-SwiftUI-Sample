
//  Color+Rainbow.swift
//  Created by Seb Jachec on 16/07/2020.

import SwiftUI

extension Color {
    static var rainbowPalette: [Color] {
        [
            Color(.systemRed),
            Color(.systemOrange),
            Color(.systemYellow),
            Color(.systemGreen),
            Color(.systemTeal),
            Color(.systemBlue),
            Color(.systemIndigo),
            Color(.systemPurple),
            Color(.systemPink)]
    }
}
